## videotube
### 1. db creation 
- can be done using 2 approach
    - approach_1
        - directly calling dbInstance in index.js, using IIFE
        - can be called using a function.
    - approach_2
        - can place the db connection code in a separate file and call the instance of a function in index.js.

- Async Function on its completion, technically returns a promise.


### 2. After Db creation Listening to the port | Starting of the server.
### 3. writing utils or helper classes


